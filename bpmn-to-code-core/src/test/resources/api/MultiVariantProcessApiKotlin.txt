// Generated by bpmn-to-code
@file:Suppress("unused")

package de.emaarco.example

import io.miragon.bpmn.runtime.AbstractFlowNode
import io.miragon.bpmn.runtime.BpmnEngine
import io.miragon.bpmn.runtime.BpmnEscalation
import io.miragon.bpmn.runtime.BpmnFlow
import io.miragon.bpmn.runtime.BpmnTimer
import io.miragon.bpmn.runtime.ElementId
import io.miragon.bpmn.runtime.HasInner
import io.miragon.bpmn.runtime.MessageName
import io.miragon.bpmn.runtime.Navigable
import io.miragon.bpmn.runtime.NavigationScope
import io.miragon.bpmn.runtime.ProcessId
import io.miragon.bpmn.runtime.VariableName
import kotlin.String
import kotlin.Suppress

object SendNewsletterProcessApi {
  val PROCESS_ID: ProcessId = ProcessId("sendNewsletter")

  val PROCESS_ENGINE: BpmnEngine = BpmnEngine.ZEEBE

  /**
   * BPMN element ids as declared in the source model.
   * Typically used in process-level tests or when searching for tasks.
   * Worker runtime code rarely needs these.
   */
  object Elements {
    val START_EVENT_EDITION_CREATED: ElementId = ElementId("startEvent_editionCreated")

    val SERVICE_TASK_LOAD_SUBSCRIBERS: ElementId = ElementId("serviceTask_loadSubscribers")

    val GATEWAY_HAS_SUBSCRIBERS: ElementId = ElementId("gateway_hasSubscribers")

    val SERVICE_TASK_SEND_TO_SUBSCRIBER: ElementId =
        ElementId("serviceTask_sendToSubscriber")

    val SERVICE_TASK_NOTIFY_AUTHOR: ElementId = ElementId("serviceTask_notifyAuthor")

    val END_EVENT_EDITION_SENT: ElementId = ElementId("endEvent_editionSent")

    val END_EVENT_NO_SUBSCRIBERS: ElementId = ElementId("endEvent_noSubscribers")

    val EVENT_SUB_PROCESS_ERROR_HANDLING: ElementId =
        ElementId("eventSubProcess_errorHandling")

    val EVENT_MAIL_REJECTED: ElementId = ElementId("event_mailRejected")

    val SERVICE_TASK_ANALYZE_ERROR: ElementId = ElementId("serviceTask_analyzeError")

    val GATEWAY_CAN_SEND_AGAIN: ElementId = ElementId("gateway_canSendAgain")

    val SERVICE_TASK_SEND_MAIL_AGAIN: ElementId = ElementId("serviceTask_sendMailAgain")

    val EVENT_GATEWAY_AFTER_SENDING_AGAIN: ElementId =
        ElementId("eventGateway_afterSendingAgain")

    val TIMER_NO_REJECTION_FOR_ONE_DAY: ElementId = ElementId("timer_noRejectionForOneDay")

    val ESCALATION_END_EVENT_NOFITY_SUPPORT: ElementId =
        ElementId("escalationEndEvent_nofitySupport")

    val EVENT_MAIL_REJECTED_AGAIN: ElementId = ElementId("event_mailRejectedAgain")

    val ESCALATION_END_EVENT_NOFITY_SUPPORT_AFTER_REPEATED_ERROR: ElementId =
        ElementId("escalationEndEvent_nofitySupportAfterRepeatedError")

    val END_EVENT_ISSUE_RESOLVED: ElementId = ElementId("endEvent_issueResolved")
  }

  /**
   * BPMN message names used to correlate messages to running process instances.
   */
  object Messages {
    val MESSAGE_MAIL_REJECTED: MessageName = MessageName("Message_MailRejected")

    val MESSAGE_MAIL_REJECTED_AGAIN: MessageName = MessageName("Message_MailRejectedAgain")
  }

  /**
   * Job worker task types used in `@JobWorker(type = ServiceTasks.X)` annotations.
   * Kept as `const val String` because annotation arguments must be compile-time constants.
   */
  object ServiceTasks {
    const val NEWSLETTER_ANALYZE_SEND_ERROR: String = "newsletter.analyzeSendError"

    const val NEWSLETTER_LOAD_SUBSCRIBERS: String = "newsletter.loadSubscribers"

    const val NEWSLETTER_NOTIFY_AUTHOR: String = "newsletter.notifyAuthor"

    const val NEWSLETTER_SEND_MAIL_TO_SUBSCRIBER: String = "newsletter.sendMailToSubscriber"
  }

  object Timers {
    val TIMER_NO_REJECTION_FOR_ONE_DAY: BpmnTimer = BpmnTimer("Duration", "PT1D")
  }

  object Escalations {
    val ESCALATION_NOTIFY_SUPPORT: BpmnEscalation =
        BpmnEscalation("escalation_notifySupport", "200")
  }

  /**
   * Process variables grouped by the BPMN element that declares them.
   * Direction is encoded in each variable's wrapper type: `VariableName.Input`, `VariableName.Output`, or `VariableName.InOut` when the variable is both read and written by the same element.
   * Consumer APIs that take a specific subtype (e.g. `fun setOutput(v: VariableName.Output)`) get compile-time direction enforcement.
   */
  object Variables {
    object ServiceTaskLoadSubscribers {
      val AUTHOR: VariableName.Output = VariableName.Output("author")

      val SUBSCRIBERS: VariableName.Output = VariableName.Output("subscribers")
    }
  }

  object Variants {
    object Send {
      /**
       * Sequence flows between BPMN elements.
       * Mainly useful for process-model tooling, tests, and AI-agent consumers reasoning about the process shape.
       * Worker code typically does not need these.
       */
      object Flows {
        val FLOW_0_BIANZ_5: BpmnFlow = BpmnFlow(
              id = "Flow_0bianz5",
              sourceRef = "startEvent_editionCreated",
              targetRef = "serviceTask_loadSubscribers",
            )

        val FLOW_04_ANDB_8: BpmnFlow = BpmnFlow(
              id = "Flow_04andb8",
              sourceRef = "serviceTask_loadSubscribers",
              targetRef = "gateway_hasSubscribers",
            )

        val FLOW_1_JOGUT_0: BpmnFlow = BpmnFlow(
              id = "Flow_1jogut0",
              name = "Yes",
              sourceRef = "gateway_hasSubscribers",
              targetRef = "serviceTask_sendToSubscriber",
              isDefault = true,
            )

        val FLOW_1_GSZ_7_WD: BpmnFlow = BpmnFlow(
              id = "Flow_1gsz7wd",
              name = "No",
              sourceRef = "gateway_hasSubscribers",
              targetRef = "endEvent_noSubscribers",
              condition = $$"""${subscribers.size() > 0}""",
            )

        val FLOW_1_RUAYVL: BpmnFlow = BpmnFlow(
              id = "Flow_1ruayvl",
              sourceRef = "serviceTask_sendToSubscriber",
              targetRef = "serviceTask_notifyAuthor",
            )

        val FLOW_0_V_2_V_55_N: BpmnFlow = BpmnFlow(
              id = "Flow_0v2v55n",
              sourceRef = "serviceTask_notifyAuthor",
              targetRef = "endEvent_editionSent",
            )

        val FLOW_0_VTPPNK: BpmnFlow = BpmnFlow(
              id = "Flow_0vtppnk",
              sourceRef = "event_mailRejected",
              targetRef = "serviceTask_analyzeError",
            )

        val FLOW_13_NMNAG: BpmnFlow = BpmnFlow(
              id = "Flow_13nmnag",
              sourceRef = "serviceTask_analyzeError",
              targetRef = "gateway_canSendAgain",
            )

        val FLOW_1_IZUCOF: BpmnFlow = BpmnFlow(
              id = "Flow_1izucof",
              name = "Yes",
              sourceRef = "gateway_canSendAgain",
              targetRef = "serviceTask_sendMailAgain",
              isDefault = true,
            )

        val FLOW_18_NF_2_JH: BpmnFlow = BpmnFlow(
              id = "Flow_18nf2jh",
              name = "No",
              sourceRef = "gateway_canSendAgain",
              targetRef = "escalationEndEvent_nofitySupport",
              condition = $$"""${rejection.reason == "PERMANENT"}""",
            )

        val FLOW_0_VYM_6_NU: BpmnFlow = BpmnFlow(
              id = "Flow_0vym6nu",
              sourceRef = "serviceTask_sendMailAgain",
              targetRef = "eventGateway_afterSendingAgain",
            )

        val FLOW_0_ENJKOE: BpmnFlow = BpmnFlow(
              id = "Flow_0enjkoe",
              sourceRef = "eventGateway_afterSendingAgain",
              targetRef = "timer_noRejectionForOneDay",
            )

        val FLOW_081_CYKL: BpmnFlow = BpmnFlow(
              id = "Flow_081cykl",
              sourceRef = "eventGateway_afterSendingAgain",
              targetRef = "event_mailRejectedAgain",
            )

        val FLOW_0_X_9_THPQ: BpmnFlow = BpmnFlow(
              id = "Flow_0x9thpq",
              sourceRef = "event_mailRejectedAgain",
              targetRef = "escalationEndEvent_nofitySupportAfterRepeatedError",
            )

        val FLOW_0338_XZF: BpmnFlow = BpmnFlow(
              id = "Flow_0338xzf",
              sourceRef = "timer_noRejectionForOneDay",
              targetRef = "endEvent_issueResolved",
            )
      }

      /**
       * Typed navigation over the process flow.
       * Each element is a node exposing its `id`, `elementType` and display `name`, plus the elements reachable from it as named properties — so a full path is verified by the compiler and offered by autocomplete. A subprocess's interior is its nested `Inner` scope.
       * Intended for tooling, tests, and reasoning about the process shape.
       */
      object Relations {
        val endEventEditionSent: EndEventEditionSent
          get() = EndEventEditionSent

        val endEventNoSubscribers: EndEventNoSubscribers
          get() = EndEventNoSubscribers

        val eventSubProcessErrorHandling: EventSubProcessErrorHandling
          get() = EventSubProcessErrorHandling

        val gatewayHasSubscribers: GatewayHasSubscribers
          get() = GatewayHasSubscribers

        val serviceTaskLoadSubscribers: ServiceTaskLoadSubscribers
          get() = ServiceTaskLoadSubscribers

        val serviceTaskNotifyAuthor: ServiceTaskNotifyAuthor
          get() = ServiceTaskNotifyAuthor

        val serviceTaskSendToSubscriber: ServiceTaskSendToSubscriber
          get() = ServiceTaskSendToSubscriber

        val startEventEditionCreated: StartEventEditionCreated
          get() = StartEventEditionCreated

        fun then(): Next = Next

        object Next {
          val startEventEditionCreated: StartEventEditionCreated
            get() = StartEventEditionCreated
        }

        object EndEventEditionSent : AbstractFlowNode(ElementId("endEvent_editionSent"), "END_EVENT")

        object EndEventNoSubscribers : AbstractFlowNode(ElementId("endEvent_noSubscribers"), "END_EVENT")

        object EventSubProcessErrorHandling : AbstractFlowNode(ElementId("eventSubProcess_errorHandling"), "SUB_PROCESS"),
            HasInner<EventSubProcessErrorHandling.Inner> {
          override fun `inner`(): EventSubProcessErrorHandling.Inner = EventSubProcessErrorHandling.Inner

          object Inner : NavigationScope<EventSubProcessErrorHandling.Inner.Next> {
            val endEventIssueResolved: EndEventIssueResolved
              get() = EndEventIssueResolved

            val escalationEndEventNofitySupport: EscalationEndEventNofitySupport
              get() = EscalationEndEventNofitySupport

            val escalationEndEventNofitySupportAfterRepeatedError:
                EscalationEndEventNofitySupportAfterRepeatedError
              get() = EscalationEndEventNofitySupportAfterRepeatedError

            val eventGatewayAfterSendingAgain: EventGatewayAfterSendingAgain
              get() = EventGatewayAfterSendingAgain

            val eventMailRejected: EventMailRejected
              get() = EventMailRejected

            val eventMailRejectedAgain: EventMailRejectedAgain
              get() = EventMailRejectedAgain

            val gatewayCanSendAgain: GatewayCanSendAgain
              get() = GatewayCanSendAgain

            val serviceTaskAnalyzeError: ServiceTaskAnalyzeError
              get() = ServiceTaskAnalyzeError

            val serviceTaskSendMailAgain: ServiceTaskSendMailAgain
              get() = ServiceTaskSendMailAgain

            val timerNoRejectionForOneDay: TimerNoRejectionForOneDay
              get() = TimerNoRejectionForOneDay

            override fun then(): Next = Next

            object Next {
              val eventMailRejected: EventMailRejected
                get() = EventMailRejected
            }

            object EndEventIssueResolved : AbstractFlowNode(ElementId("endEvent_issueResolved"), "END_EVENT")

            object EscalationEndEventNofitySupport : AbstractFlowNode(ElementId("escalationEndEvent_nofitySupport"), "ESCALATION_END_EVENT")

            object EscalationEndEventNofitySupportAfterRepeatedError : AbstractFlowNode(ElementId("escalationEndEvent_nofitySupportAfterRepeatedError"), "END_EVENT")

            object EventGatewayAfterSendingAgain : AbstractFlowNode(ElementId("eventGateway_afterSendingAgain"), "EVENT_BASED_GATEWAY"),
                Navigable<EventGatewayAfterSendingAgain.Next> {
              override fun then(): Next = Next

              object Next {
                val eventMailRejectedAgain: EventMailRejectedAgain
                  get() = EventMailRejectedAgain

                val timerNoRejectionForOneDay: TimerNoRejectionForOneDay
                  get() = TimerNoRejectionForOneDay
              }
            }

            object EventMailRejected : AbstractFlowNode(ElementId("event_mailRejected"), "MESSAGE_START_EVENT"),
                Navigable<EventMailRejected.Next> {
              override fun then(): Next = Next

              object Next {
                val serviceTaskAnalyzeError: ServiceTaskAnalyzeError
                  get() = ServiceTaskAnalyzeError
              }
            }

            object EventMailRejectedAgain : AbstractFlowNode(ElementId("event_mailRejectedAgain"), "MESSAGE_INTERMEDIATE_CATCH_EVENT"),
                Navigable<EventMailRejectedAgain.Next> {
              override fun then(): Next = Next

              object Next {
                val escalationEndEventNofitySupportAfterRepeatedError:
                    EscalationEndEventNofitySupportAfterRepeatedError
                  get() = EscalationEndEventNofitySupportAfterRepeatedError
              }
            }

            object GatewayCanSendAgain : AbstractFlowNode(ElementId("gateway_canSendAgain"), "EXCLUSIVE_GATEWAY"),
                Navigable<GatewayCanSendAgain.Next> {
              override fun then(): Next = Next

              object Next {
                val escalationEndEventNofitySupport: EscalationEndEventNofitySupport
                  get() = EscalationEndEventNofitySupport

                val serviceTaskSendMailAgain: ServiceTaskSendMailAgain
                  get() = ServiceTaskSendMailAgain
              }
            }

            object ServiceTaskAnalyzeError : AbstractFlowNode(ElementId("serviceTask_analyzeError"), "SERVICE_TASK"),
                Navigable<ServiceTaskAnalyzeError.Next> {
              override fun then(): Next = Next

              object Next {
                val gatewayCanSendAgain: GatewayCanSendAgain
                  get() = GatewayCanSendAgain
              }
            }

            object ServiceTaskSendMailAgain : AbstractFlowNode(ElementId("serviceTask_sendMailAgain"), "SERVICE_TASK"),
                Navigable<ServiceTaskSendMailAgain.Next> {
              override fun then(): Next = Next

              object Next {
                val eventGatewayAfterSendingAgain: EventGatewayAfterSendingAgain
                  get() = EventGatewayAfterSendingAgain
              }
            }

            object TimerNoRejectionForOneDay : AbstractFlowNode(ElementId("timer_noRejectionForOneDay"), "TIMER_INTERMEDIATE_CATCH_EVENT"),
                Navigable<TimerNoRejectionForOneDay.Next> {
              override fun then(): Next = Next

              object Next {
                val endEventIssueResolved: EndEventIssueResolved
                  get() = EndEventIssueResolved
              }
            }
          }
        }

        object GatewayHasSubscribers : AbstractFlowNode(ElementId("gateway_hasSubscribers"), "EXCLUSIVE_GATEWAY"),
            Navigable<GatewayHasSubscribers.Next> {
          override fun then(): Next = Next

          object Next {
            val endEventNoSubscribers: EndEventNoSubscribers
              get() = EndEventNoSubscribers

            val serviceTaskSendToSubscriber: ServiceTaskSendToSubscriber
              get() = ServiceTaskSendToSubscriber
          }
        }

        object ServiceTaskLoadSubscribers : AbstractFlowNode(ElementId("serviceTask_loadSubscribers"), "SERVICE_TASK"),
            Navigable<ServiceTaskLoadSubscribers.Next> {
          override fun then(): Next = Next

          object Next {
            val gatewayHasSubscribers: GatewayHasSubscribers
              get() = GatewayHasSubscribers
          }
        }

        object ServiceTaskNotifyAuthor : AbstractFlowNode(ElementId("serviceTask_notifyAuthor"), "SERVICE_TASK"),
            Navigable<ServiceTaskNotifyAuthor.Next> {
          override fun then(): Next = Next

          object Next {
            val endEventEditionSent: EndEventEditionSent
              get() = EndEventEditionSent
          }
        }

        object ServiceTaskSendToSubscriber : AbstractFlowNode(ElementId("serviceTask_sendToSubscriber"), "SERVICE_TASK"),
            Navigable<ServiceTaskSendToSubscriber.Next> {
          override fun then(): Next = Next

          object Next {
            val serviceTaskNotifyAuthor: ServiceTaskNotifyAuthor
              get() = ServiceTaskNotifyAuthor
          }
        }

        object StartEventEditionCreated : AbstractFlowNode(ElementId("startEvent_editionCreated"), "START_EVENT"),
            Navigable<StartEventEditionCreated.Next> {
          override fun then(): Next = Next

          object Next {
            val serviceTaskLoadSubscribers: ServiceTaskLoadSubscribers
              get() = ServiceTaskLoadSubscribers
          }
        }
      }
    }
  }
}
